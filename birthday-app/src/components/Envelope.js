import React from "react";

const Envelope = () => (
  <div className="envelope-overlay">
    <img src="/envelope.gif" alt="Opening Envelope" className="envelope-gif" />
  </div>
);

export default Envelope;
